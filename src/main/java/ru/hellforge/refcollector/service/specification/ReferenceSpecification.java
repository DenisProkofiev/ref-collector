package ru.hellforge.refcollector.service.specification;

/**
 * ReferenceSpecification.
 *
 * @author dprokofev
 */

public class ReferenceSpecification {
//
//  public static Specification<Reference> getSpecification(ReferenceFilter filter) {
//    return (root, query, criteriaBuilder) -> buidPredicate(root, query, criteriaBuilder);
//  }
//
//  private static Predicate buildPredicate(Root<Reference> root, CriteriaQuery<?> query, CriteriaBuilder criteriaBuilder) {
//    Root<Reference> referenceRoot = query.from(Reference.class);
//  }
//
//
//  private static List<Predicate<Reference>> getPredicateList(List<TagDto> tagList, CriteriaBuilder criteriaBuilder, Root<Reference> root) {
//
//    return tagList.stream()
//        .map(tag -> criteriaBuilder.equal(root.get("na"),tag.getName()))
//        .collect(Collectors.toList());
//  }

}
